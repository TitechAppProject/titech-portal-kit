name: Swift

on: [push]

jobs:
  test:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: swift build -Xswiftc -enable-bare-slash-regex
      - name: Run tests
        run: swift test
